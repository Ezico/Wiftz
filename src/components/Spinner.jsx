import React from "react";

const Spinner = () => {
  return (
    <div role="status" className="spinner spinner-border text-primary mt-5">
      <span className="visually-hidden">Loading</span>
    </div>
  );
};

export default Spinner;
